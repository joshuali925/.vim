---
description: Commit changes using conventional commits format
model: haiku
---

Commit ONLY YOUR CHANGES to git, add pre-existing diffs only if user specifies. Use the `--signoff` flag. Use conventional commits format with no emojis, keep the commit title simple and high level, not implementation details. Don't add unnecessary descriptions.

$ git log --oneline -n 5
!`git log --oneline -n 5`

$ git status
!`git status`
